import 'package:flutter/material.dart';

class AccountUpdateProfileWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Text('UPDATE PROFILE'),);
  }
}
